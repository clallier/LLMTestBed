[project]
name = "llm-testbed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "pydantic",
    "streamlit",
    "python-dotenv",
    "langfuse",
    "pytest",
    "pytest-asyncio",
    "respx",
    "watchdog"
]
